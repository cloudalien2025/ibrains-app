name: CI (DirectoryIQ)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This workflow has been retired. Use the main CI workflow instead."
