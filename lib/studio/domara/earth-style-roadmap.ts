export function earthStyleRoadmapMessage(): string {
  return "Earth-style sequence planned: Google Earth Studio is a browser animation workflow; CasaFlix supports placeholder/import seams rather than a live Earth video API.";
}
